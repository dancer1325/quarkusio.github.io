////
This document is maintained in the main Quarkus repository, and pull requests should be submitted there:
https://github.com/quarkusio/quarkus/tree/main/docs/src/main/asciidoc
////
[id="security-identity-providers"]
= Identity providers
include::_attributes.adoc[]
:diataxis-type: concept
:categories: security
:topics: security,identity-providers
:extensions: io.quarkus:quarkus-elytron-security-jdbc,io.quarkus:quarkus-elytron-security-ldap,io.quarkus:quarkus-security-jpa-reactive

* Identity providers -- `IdentityProvider` --
    ** 👀creates a `SecurityIdentity` instance 👀
        *** FROM authentication credentials / provided -- by -- `HttpAuthenticationMechanism`
        *** used |
            **** authenticate -- to -- verify
                ***** _Example:_ if you use Basic OR form-based authentication -> you must add an `IdentityProvider` instance / convert a username & password -- to a -- `SecurityIdentity` instance
            **** authorize access requests | your Quarkus application
    ** 👀some extensions have inline `IdentityProvider` implementations👀
        *** -- for -- supported authentication flow
        *** _Examples:_
            **** `quarkus-oidc`
                ***** 's own `IdentityProvider` / convert a token -- to a -- `SecurityIdentity` instance
    ** recommendations
        *** | get started with security in Quarkus,
            **** if you want to enable role-based access control (RBAC) -> use Quarkus built-in Basic HTTP authentication + Jakarta Persistence identity provider

* Basic authentication mechanisms & related identity providers
    ** xref:security-jpa.adoc[Quarkus Security with Jakarta Persistence]
        *** xref:security-getting-started-tutorial.adoc[Getting started with Security by using Basic authentication and Jakarta Persistence]
    ** xref:security-authentication-mechanisms.adoc#form-auth[Form-based authentication]
    ** xref:security-jdbc.adoc[Using security with JDBC]
    ** xref:security-ldap.adoc[Using security with an LDAP realm]
    ** xref:security-overview.adoc[Quarkus Security overview]
